/**
 * Main Mediator
 * 
 * Mediates and coordinates between the contracts required to support the basic 
 * data structures needed by the App (communities, persons, claims, etc...), 
 * the onchain MINA state, and the updates and indexes to offchain data server.
 * 
 * created: mario@socialcapp.app - 2023-06-23
 */
import { RootContract } from '../RootContract.js';

