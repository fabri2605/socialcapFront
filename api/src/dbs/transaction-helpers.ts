/**
 * Manages storage and retrieval of Entity transactions involving MINA
 */
import { prisma } from "../global.js";

async function getTransaction(
  txid: string
): Promise<any> {
  return {}  
}

async function saveTransaction(
  txid: string, 
  state: string, 
  obj: any
): Promise<any> {
  return {}  
}